# ===========================================
# AWS PRODUCTION ENVIRONMENT CONFIGURATION
# ===========================================

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
DB_TYPE=mysql

# AWS RDS MySQL Configuration
# RDS Instance: food-ordering-db
# Database Name: food_ordering (created inside the RDS instance)
DB_HOST=food-ordering-db.cjmqw882g6hb.us-east-1.rds.amazonaws.com
DB_USER=admin
DB_PASSWORD=FoodOrder2024!
DB_NAME=food_ordering
DB_PORT=3306

# Connection Pool Settings (for production optimization)
DB_CONNECTION_LIMIT=10
DB_ACQUIRE_TIMEOUT=60000
DB_TIMEOUT=60000

# JWT Configuration
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secure_jwt_secret_key_change_this_now_64_chars_minimum
JWT_EXPIRES_IN=24h

# Frontend Configuration
# Updated with your EC2 IP
FRONTEND_URL=http://44.210.235.221:3000

# CORS Configuration
CORS_ORIGIN=http://44.210.235.221:3000
# For multiple origins: CORS_ORIGIN=http://your-ec2-ip:3000,https://yourdomain.com

# AWS Configuration (if using AWS SDK features)
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key (use IAM roles instead)
# AWS_SECRET_ACCESS_KEY=your-secret-key (use IAM roles instead)

# Security Configuration
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Session Configuration
SESSION_SECRET=another_super_secure_session_secret_key_change_this